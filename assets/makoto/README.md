# Archived transparent assets

The top page no longer loads transparent Makoto layers from this directory.
Completed room backgrounds now live in `assets/images/` and are selected by
`js/room.js`.

Existing files are preserved here only as unused source material.
