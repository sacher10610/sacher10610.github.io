# Illustration assets

Place finished illustration files in this directory and reference them from
`/js/illustration.js` with a root-relative path such as:

```js
src: "/assets/illustrations/illustration-001.webp"
```

WebP or AVIF is recommended. Keep the optional `ratio` value aligned with the
source artwork to reserve the correct layout space before it loads.
